name: jira_add_on
description: Command-line tool to log working hours on Jira.
version: 0.9.0

environment:
  sdk: ^3.4.1

dependencies:
  args: ^2.5.0
  deep_pick: ^1.0.0
  dio: ^5.5.0+1
  dotenv: ^4.2.0
  interact: ^2.2.0
  intl: ^0.18.1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0

executables:
  jira_add_on:
